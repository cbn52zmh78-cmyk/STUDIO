# System Prompts

STUDIO-layer system and session prompts — director workflow rules, production constraints, and behavior loaded at session start.

**Use when:** Configuring how Grok or a director agent behaves inside STUDIO (not per-scene generation).

**Not for:** Category-specific scene prompts (see `../library/` and domain folders like `astrophysics/`).

Keep version headers on material changes. Align with locked rules in `../../Canons/` when applicable.